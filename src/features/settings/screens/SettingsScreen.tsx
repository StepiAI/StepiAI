import { Text, View } from 'react-native';

export function SettingsScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-background">
      <Text className="text-body text-textMuted">Settings — coming soon</Text>
    </View>
  );
}
